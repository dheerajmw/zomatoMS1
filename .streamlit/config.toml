# Shared theme: Streamlit Cloud and local `streamlit run` both read this when present at repo root.
[browser]
gatherUsageStats = false

[theme]
primaryColor = "#E23744"
backgroundColor = "#FAFAFA"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#1C1C1C"
font = "sans serif"
