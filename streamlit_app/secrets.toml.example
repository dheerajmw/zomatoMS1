# Copy to `.streamlit/secrets.toml` in the repo root when running locally (file is gitignored).
# Streamlit Community Cloud: set the same key in the dashboard Secrets UI.

API_BASE_URL = "http://127.0.0.1:8000"
